/**
 * End-to-end offline simulator: drives the emulator + oracle without any
 * Solana connection so anyone can run `npm run simulate` and watch the
 * probability curve evolve.
 */
import "./index.js";
